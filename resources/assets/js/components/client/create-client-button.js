var React = require('react');

module.exports = React.createClass({

    render: function() {
        return(
        	<button className="btn btn-primary create-client-button">Create new client</button>
        );
    }

});